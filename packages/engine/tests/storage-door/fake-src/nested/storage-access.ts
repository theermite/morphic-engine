// Fixture: a decoy wearing a gatekeeper's name, one folder down.
//
// The sweep used to skip it, because it compared the file NAME. A module hidden
// here reached localStorage with fourteen tests green (review of 2026-09-03).
// It must be scanned.
export const marker = 'decoy, must be scanned';
