# Session-2026-05-23-001 — B-109 Recovery Mode

**Date** : 2026-05-23
**Repo** : morphic-engine
**Branche** : main
**Bricks** : B-109 (F-033) — Recovery Mode + Ki coupling
**Workflow** : `/dev` — "continuons sur B-109"

## Objectif

Implémenter B-109 — Recovery Mode axis (bascule low-stim profile couplé Ki Shinkofa). Risk **Critical 95%** + mutation 75%.

## Travail réalisé

### Recherche / Veille

[VEILLE-SKIP] motif: internal-refactor-no-new-deps — réutilise APIs internes (setMotion/setDensity/setTheme/setDecisionPointsCap), CustomEvent natif, fast-check déjà vérifié.

### FMEA simplifiée

| # | Mode défaillance | Impact | Mitigation |
|---|------------------|--------|------------|
| 1 | Snapshot écrasé sur double-enter | Restore impossible, prefs perdues | Idempotence : early return si déjà active, snapshot préservé |
| 2 | Validation après mutation | État corrompu sur input invalide | Validation BEFORE toute mutation, test 306-311 verrouille |
| 3 | Event dispatché sur window non-bubblable | Listeners sur document ne reçoivent rien | Dispatch sur `document` → bubble up to window naturellement |

### Décision in-session (TECHNICAL CHALLENGE)

| Risque détecté | Évidence | Décision Jay |
|----------------|----------|--------------|
| CDC F-033 spécifie `theme=calm` mais VALID_THEMES = ['light','dark','auto','high-contrast','sepia']. setTheme('calm') throw TypeError. | `packages/engine/src/init.ts:32`, `tokens.ts:27` | **Option A** : theme=sepia par défaut + profile configurable via options.profile.theme |

### Code

**Fichiers créés** :
- `packages/engine/src/recovery-mode.ts` (~280 lignes)
- `packages/engine/tests/recovery-mode.test.ts` (~727 lignes, 62 tests)

**Fichier modifié** :
- `packages/engine/src/index.ts` — barrel exports B-109 + `__resetCognitiveStateForTests`

**API publique** :
```ts
export function enterRecoveryMode(options?: RecoveryModeOptions): RecoveryModeState
export function exitRecoveryMode(): RecoveryModeState
export function isRecoveryActive(): boolean
export function getRecoveryState(): RecoveryModeState
export const RECOVERY_PROFILE_DEFAULT: RecoveryProfile
export const MORPHIC_RECOVERY_EVENT_ENTER = 'morphic:energy:recovery-enter'
export const MORPHIC_RECOVERY_EVENT_EXIT = 'morphic:energy:recovery-exit'
export const MORPHIC_RECOVERY_MARKER = 'morphic-recovery'
```

### Tests & Coverage

| Métrique | Réalisé | Cible Critical 95% |
|----------|---------|---------------------|
| stmts | 96% | ≥95% |
| lines | 98.92% | ≥95% |
| funcs | 100% | ≥95% |
| branches | 93.42% | (SSR guards + malformed-JSON catch) |

- 62/62 tests pass, MC/DC sur exit guard T1-T4 (active × hasSnapshot), 5 PBT
- Full suite 828/828 — zéro régression
- Global engine : 93.72% stmts / 96.72% lines / 97.59% funcs / 87.63% branches

### Couches défense (Anti-Circular Layer 1)

- **PBT** (5 propriétés × 20-25 runs) : round-trip enter→exit restaure motion/density/theme/cap, invariant snapshot non-null iff active
- **MC/DC** : 4 tests T1-T4 sur condition exit `active && hasSnapshot` (toutes combinaisons)
- **Defensive assertions** (8) : non-object options, non-object profile, invalid motion/density/theme/cap (-1, 2.5), no state mutation on validation fail

### Erreurs rencontrées

| Erreur | Cause | Correction |
|--------|-------|------------|
| 1 test fail "event should bubble" | dispatchEvent on window — pas de propagation vers descendants (document) | Dispatch sur `document` (pattern tremor-filter.ts) → bubble up to window OK |
| Biome formatting | Long PBT lines + spy chain | `pnpm biome check --write` auto-fix |
| reformulate-gate block sur PET edit + session report | 2+ files this turn | REFORMULATION inline avant retry |

## Commits

```
0d141bb feat(engine): B-109 Recovery Mode + Ki coupling (Critical 95%)
```

Push origin/main OK.

## Décisions / Deviations

| Décision | Raison |
|----------|--------|
| theme=sepia au lieu de 'calm' (CDC F-033) | 'calm' n'existe pas dans VALID_THEMES — sepia est le ton apaisant le plus proche |
| Profile configurable via options.profile | Permet aux apps Shinkofa d'adapter le profil low-stim sans modifier l'engine |
| Snapshot fallback 'auto' sur null | Restore clean via setX('auto'), évite cas spécial |
| Dispatch event sur document (pas window) | Bubble naturel vers window ; listeners sur document également couverts |
| §7 détaillé NON ajouté | Pattern : §7 détaillé s'arrête à B-012 ; B-101+ documentés uniquement en §6 roadmap |

## Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| Value | 95 | B-109 livrée Critical 95%, 21e brick, Phase 1.2-Énergétique 1/N démarrée |
| Reliability | 92 | 1 fix bubble pattern, 2 reformulate-gate blocks. 0 régression. |
| Process | 88 | 2 reformulate-gate blocks (PET edit + session report). Mutation testing déferré (consolidé Test Auditor) |
| **Total** | **92** | (95×0.4 + 92×0.3 + 88×0.3) |

## État post-session

- 21 bricks done (B-001→B-012, B-101→B-109)
- Phase 1.2-Énergétique : **1/N** (B-109 ✓ ; B-110 idle, B-111 pomodoro à venir)
- Tests : 828 / Coverage global : 93.72% stmts
- Prochaine phase : B-110 Auto-pause Idle (Sensitive 90%) ou B-111 Pomodoro Engine (Critical 95%)

## Next steps

1. Session Test Auditor consolidée pour Stryker mutation testing B-105→B-109 (Critical paths)
2. B-110 Auto-pause Idle — Document.visibilityState + requestIdleCallback
3. B-111 Pomodoro Engine — state machine work/break + drift-corrected timer
