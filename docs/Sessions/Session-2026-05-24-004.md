# Session-2026-05-24-004 — B-021g v5 rollback + Plan NLNet recalibration

**Date** : 2026-05-24
**Duree** : ~2h (continuation post-compaction x2)
**Machine** : VPS Shinkofa (ubuntu@217.182.206.127)
**Branche** : `main` (morphic-engine + The-Ermite)

---

## Travail realise

### B-021g v5 — Rollback scope global + chrome-safe (The-Ermite)

**Probleme** : Session-003 avait scope les attributs morphic dans un `<div class="morphic-preview-root">`. Resultat : les axes qui modifient `document.documentElement` (theme, fontSize, fontFamily, contrast, density, motion) ne fonctionnaient plus visuellement hors du preview div. La majorite des axes (6/18 principaux) etaient casses.

**Solution appliquee (Option B — approuvee par Jay)** :
- Revert du scoping preview div → retour scope global `document.documentElement`
- Suppression `setTarget()` usage cote consumer (l'API existe toujours cote engine)
- CSS chrome-safe : `revert !important` sur header/nav pour exclure le chrome site
- Cleanup imports inutiles (6 setters supprimes)
- Correction `useEffect` manquant dans imports React (casse 5 tests)

**Deploy** : Docker The-Ermite, container healthy, smoke tests OK.
**Commit The-Ermite** : `ac658e0`

### Plan NLNet Submission — creation + recalibration

**Cree** : `docs/Plan-NLNet-Submission.md` — sprint plan complet pour maximiser NLNet NGI0 Commons.
- 4 blocs : engine (2h), demo Lab (2h30), chrome-safe+widget (3h30), dossier (4h30)
- Estimations recalibrees apres challenge Jay : 24h → 12h30 total focus (basees sur rythme observe ~35 bricks/3.5j)
- Verdict NLNet : 40-50% avant corrections → 70-80% apres plan execute

### Stale files commites

- `.npmrc`, `packages/adapter/LICENSE`, `packages/engine/LICENSE`, `packages/engine/README.md`
- `packages/adapter/package.json` + `pnpm-lock.yaml`

---

## Tests

| Package | Pass | Fail | Notes |
|---------|------|------|-------|
| morphic-engine | 201 | 684 | **PRE-EXISTANT** : `localStorage is not defined` dans 28/35 fichiers test. Identique avec/sans mes changes (verifie via `git stash`). Session-003 citait 1248 pass — config jsdom cassee entre sessions. |
| The-Ermite | 340 | 0 | Verifie en session (5 tests MorphicLab pass apres fix useEffect) |

---

## Erreurs rencontrees

1. **5 tests MorphicLab `useEffect is not defined`** — cause : useEffect retire des imports React lors du rollback. Fix : re-ajoute useEffect a l'import.
2. **Reformulate-gate hook** — bloque a chaque edit (detecte 2+ fichiers meme sur fichier unique). Contourne par REFORMULATION systematique.
3. **Veille hook** — bloque edit tsx (trigger `new external import`). Fix : `[VEILLE] react@19.2.4` avec source package.json local.
4. **6 lint warnings imports inutiles** — setters Phase 2 devenus orphelins apres rollback. Fix : supprimes.
5. **Smoke test port 3008** — mauvais port, le vrai est 3019. Fix : `docker port` puis URL publique.
6. **684 tests engine en echec** — decouverte en fin de session. Pre-existant (HEAD identique). Root cause : absence `// @vitest-environment jsdom` ou config globale jsdom manquante. A investiguer session prochaine.

---

## Decisions

- **Option B (rollback scope global)** choisie plutot qu'Option A (fix setTarget) car : majorite des axes cassees, delai NLNet, complexite de re-wirer 6 setters dans l'adapter. Le scoping par setTarget reste disponible engine-side pour usage futur.
- **Estimations recalibrees** suite au challenge de Jay : rythme reel = ~35 bricks/3.5j calendaires, pas les estimations conservatrices initiales.
- **684 tests fails documentes comme pre-existants** — pas de regression introduite cette session. A investiguer (config jsdom) en prochaine session priorite basse — pas bloquant pour le dev NLNet.

---

## Pending items (prochaine session)

1. **Bloc 1** : B-024b Delete GDPR (~1h30) + B-021d SSR-safe (~30min)
2. **Bloc 2** : Lab Reading Focus UI (~45min) + WAI resolver mock (~45min) + badges behavioral (~1h)
3. **Bloc 3** : Chrome-safe colorVision/Guide (~1h30) + Feedback Widget D25 (~2h)
4. **Bloc 4** : Article pillar (~1h30) + Audit GO/NO-GO (~30min) + Dossier NLNet (~2h30)
5. **Bug test engine** : 684 tests en echec `localStorage is not defined` (config jsdom)
6. **Stale files commites** : FAIT (commit 8ed4c92)

**Plan complet** : `docs/Plan-NLNet-Submission.md`
**Deadline** : 2026-06-01 (NLNet NGI0 Commons)

---

## Context Awareness

- Session continuee apres 2 compactions successives.
- Degradation mineure observable (reformulate-gate faux positifs sur fichier unique).
- Estimation ~50% contexte au moment de cloturer. Session fraiche recommandee (confirmee par Jay).

---

## Visibility check

**Candidat** : article "Scoping DOM vs global adaptation — the setTarget() lesson" (combine Sessions 002+003+004). Case study complet : API design → consumer wiring → rollback → chrome-safe pattern. **Format** : blog post Dev.to/The-Ermite. **Statut** : idee.

---

## Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| Value | 80 | B-021g v5 deploye + Plan NLNet complet + stale files commites. Pas de nouvelle feature engine livree. |
| Reliability | 75 | 5 corrections (useEffect, lint, port, hook blocks x2). Zero regression introduite. -10 684 tests fails decouverts tardivement (pre-existants mais non signales avant). -5 estimation initiale gonflee. |
| Process | 80 | TDG respecte (tests verifies avant/apres). Atomic commits. Reformulations emises. -10 estimations gonflee necessitant recalibration. -5 2 compactions successives (session longue). |
| **Total** | **79** | (80x0.4)+(75x0.3)+(80x0.3) = 32+22.5+24 = **78.5 → 79** |

---

## 5S + Monozukuri : 5/6

1. **S1** — Zero TODO/console.log ajoute. Stale files commites (nettoyage S1). Imports morts supprimes.
2. **S2** — Plan-NLNet a sa place dans `docs/`. Commits conventionnels.
3. **S3** — Tests The-Ermite 340/340 verts. Lint clean sur scope. Tests engine pre-existants documentes.
4. **S4** — Conventions respectees (REFORMULATION, VEILLE, Conventional Commits).
5. **S5** — Discipline tenue malgre 2 compactions.
6. **Bonus** — NON : 684 tests engine fails a investiguer (regle manquante sur config jsdom globale).

---

## Marker veille

`[VEILLE] react@19.2.4 verifie 2026-05-24 via /home/ubuntu/apps/The-Ermite/apps/the-ermite/package.json`
