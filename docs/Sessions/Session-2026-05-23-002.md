# Session-2026-05-23-002 — B-110 Auto-pause Idle

**Date** : 2026-05-23
**Repo** : morphic-engine
**Branche** : main
**Bricks** : B-110 (F-034) — Auto-pause Idle (visibilitychange + idle timeout)
**Workflow** : `/dev` — "on enchaine sur B-110"

## Objectif

Implémenter B-110 — Axe énergétique Auto-pause Idle. Détection inactivité ≥ idleMs (défaut 60s) + visibilitychange (tab hidden). Risk **Sensitive 90%**.

## Travail réalisé

### Recherche / Veille

[VEILLE-SKIP] motif: internal-refactor-no-new-deps — réutilise APIs natives (`Document.visibilityState`, `setTimeout`, `CustomEvent`, `addEventListener`) + pattern interne déjà validé sur B-109 (storage sub-key, dispatch document+bubbles).

### FMEA simplifiée

| # | Mode défaillance | Impact | Mitigation |
|---|------------------|--------|------------|
| 1 | Timer continue après `clearIdleDetection()` → emit fantôme | Pause-suggested intempestif post-clear | `clearTimeout` + `currentState.enabled === false` re-check dans callback |
| 2 | visibilitychange (hidden) + timer expire simultané → double-emit pause | Listeners reçoivent 2× pause, état UI désynchronisé | Race-safe : timer callback check `currentState.idle` avant emit ; visibilitychange annule le timer |
| 3 | Listeners double-registered si set 2× → activity emit 2× resume | Memory leak + double events | `listenersAttached` flag idempotent (attach early-return si déjà attached) |

### Décision in-session

| Décision | Raison |
|----------|--------|
| `requestIdleCallback` ABANDONNÉ vs PET initial | Spec WHATWG `Document.visibilityState` + `setTimeout` couvre 100% du besoin F-034 ; `requestIdleCallback` ajoute complexité (low-priority queue) sans bénéfice pour détection "user idle" (vs "main-thread idle"). |
| `idleMs` bornes [10s..10min] | Hors-bornes = no-sense (10s = trop court → spam, 10min = utilisateur déjà parti depuis longtemps). RangeError explicite. |
| Pattern aligné B-109 recovery-mode | Storage sub-key, dispatch `document` avec `bubbles:true`, SSR guards, validation-before-mutation. Cohérence axe énergétique. |

### Code

**Fichiers créés** :
- `packages/engine/src/idle-detection.ts` (~368 lignes)
- `packages/engine/tests/idle-detection.test.ts` (~550 lignes, 54 tests)

**Fichier modifié** :
- `packages/engine/src/index.ts` — barrel exports B-110 (13 symboles)

**API publique** :
```ts
export function setIdleDetection(options?: IdleDetectionOptions): IdleDetectionState
export function clearIdleDetection(): IdleDetectionState
export function isIdle(): boolean
export function getIdleDetectionState(): IdleDetectionState
export const IDLE_TIMEOUT_DEFAULT = 60_000
export const IDLE_TIMEOUT_MIN = 10_000
export const IDLE_TIMEOUT_MAX = 600_000
export const MORPHIC_IDLE_EVENT_PAUSE = 'morphic:energy:pause-suggested'
export const MORPHIC_IDLE_EVENT_RESUME = 'morphic:energy:resume'
export const MORPHIC_IDLE_MARKER = 'morphic-idle'
```

### Tests & Coverage

| Métrique | Réalisé | Cible Sensitive 90% |
|----------|---------|---------------------|
| stmts | 91.39% | ≥90% |
| lines | 99.22% | ≥90% |
| funcs | 100% | ≥90% |
| branches | 85.04% | (SSR guards + malformed-payload catch) |

- 54/54 tests pass (constants 5, happy path 4, defensive 7, timer 5, activity types 4, resume 4, visibility 3, clear 4, replacement 2, persistence 5, read API 2, type contract 1, edge paths 8)
- Full suite **882/882** — zéro régression (828 → 882, +54)

### Couches défense (Sensitive 90%)

- **Defensive assertions** (7) : non-object options, non-boolean `enabled`, non-finite/non-integer/non-number `idleMs`, hors-bornes [MIN..MAX] (low + high). State NOT mutated when validation fails.
- **Race-safe** : timer callback re-check `currentState.idle` (no duplicate emit if visibilitychange already paused) ; activity handler re-check `currentState.enabled`.
- **Idempotence** : `clearIdleDetection()` safe when not active ; `__resetIdleDetectionStateForTests()` detaches + clears.
- **Replacement** : `setIdleDetection` 2× → listeners NOT double-registered (`listenersAttached` flag).
- **SSR-safe** : 4 guards (`document`, `window`, `CustomEvent`, `localStorage`).
- **Storage corruption-tolerant** : `isPlainObject` + try/catch JSON parse, out-of-bounds stored.idleMs ignoré.

### Erreurs rencontrées

| Erreur | Cause | Correction |
|--------|-------|------------|
| reformulate-gate block (×3) sur Write/Edit | Hook BLOCKING 2+ fichiers/turn | REFORMULATION inline (file impacted + intent) avant retry |
| 1 test fail `__reset should clear active timer` | Storage persistait `enabled:true`, reload sur `getIdleDetectionState()` post-reset | Test refondu : `localStorage.clear()` post-reset puis vérification listener pause NON appelé |
| Coverage stmts 88.07% initial < Sensitive 90% | 11 lignes branches non couvertes (edge paths storage + visibility) | +8 edge-path tests → 91.39% stmts |
| Biome lint 1 error | Trailing whitespace ligne 221 idle-detection.ts | `pnpm biome check --write` auto-fix |

## Commits

```
3862f55 feat(engine): B-110 Auto-pause Idle (Sensitive 90%)
```

Push origin/main OK.

## Décisions / Deviations

| Décision | Raison |
|----------|--------|
| `requestIdleCallback` retiré du scope (vs PET initial) | Inutile pour "user idle" — `setTimeout(idleMs)` + activity events suffisent |
| `idleMs` borné [10_000..600_000] | Poka-yoke : hors-bornes n'a aucun cas d'usage légitime |
| Bornes en `as const` exportées | Permet aux consommateurs (Shinkofa-Shared) de réutiliser les bornes pour leurs propres validations UI |
| `__resetIdleDetectionStateForTests` exporté | Pattern aligné B-105 (cognitive) + B-109 (recovery) — test isolation propre |

## Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| Value | 92 | B-110 livrée Sensitive 90%, 22e brick, Phase 1.2-Énergétique 2/N |
| Reliability | 90 | 3 reformulate-gate blocks (résolus), 1 test refondu, 1 lint auto-fix. 0 régression. |
| Process | 88 | 3 reformulate-gate blocks (PET workflow 2+ files turn). Veille SKIP justifié (motif enum-valid). |
| **Total** | **90** | (92×0.4 + 90×0.3 + 88×0.3) |

## État post-session

- **22 bricks done** (B-001→B-012, B-101→B-110)
- Phase 1.2-Énergétique : **2/N** (B-109 ✓, B-110 ✓ ; B-111 Pomodoro à venir)
- Tests : **882** / Coverage global : ≥93.7% stmts
- Prochaine brick : **B-111 Pomodoro Engine** (Critical 95% + mutation 75%) — state machine work/break + drift-corrected timer via `performance.now()`

## Next steps

1. B-111 Pomodoro Engine — state machine work/break + events tick/work-end/break-start/break-end/session-complete
2. Session Test Auditor consolidée Stryker mutation B-105→B-110 (Critical paths uniquement)
3. Optionnel : adapter @morphic/adapter (B-008+) pour exposer `useIdleDetection()` hook React
