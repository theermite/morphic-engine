# Session 2026-05-23-008 — B-019 Effect-TS Resilience Layer

> CDC ref : F-018 — Effect-TS resilience, algebraic effects, structured errors
> Brick : B-019 (Sensitive 90%)
> Repo : morphic-engine (PUBLIC, AGPL)
> Statut : 🟢 Done

---

## Contexte

Suite de Session-2026-05-23-007 (B-018 Rust→WASM closed, tag `morphic-v2.0.0-brick-018`). Demande Jay : « ok passons au B-019 ». Risk classification CDC §7 : Sensitive → 90% coverage cible.

## TECHNICAL CHALLENGE (Gate 1)

Effect-TS hard-dep refacto aurait multiplie le bundle core par ~6 (engine ~8 KB + Effect ~50 KB). Trade-off presente :

| Option | Bundle core | Refacto | Choix |
|--------|-------------|---------|-------|
| A. Hard-dep refacto toutes API | +50 KB | Massif (B-001→B-018 retouchees) | ❌ |
| B. Opt-in subpath peerDep optional | +0 KB | Zero (core intact) | ✅ Jay |

Reponse Jay : « Allons pour la deuxième option. »

## Travail realise

### Architecture livree

- **Subpath export** `@morphic/engine/effects` declare dans `package.json` (exports + peerDependencies + peerDependenciesMeta.optional)
- **4 modules sources** sous `packages/engine/src/effects/` :
  - `errors.ts` — `StorageError` + `CryptoError` via `Data.TaggedError`
  - `storage.ts` — 5 wrappers (`persist`/`load`/`clear`/`migrate`/`status`) → `Effect.Effect<A, StorageError>`
  - `crypto.ts` — `getCryptoBackend` (total, `never` error) + `loadWasmBackend` (partial, `CryptoError`)
  - `index.ts` — namespaces `Storage` + `Crypto`, errors flat re-export
- **3 fichiers tests** sous `packages/engine/tests/effects/` (20 tests : 5+10+5)

### Verifications

| Gate | Methode | Resultat |
|------|---------|----------|
| 1. Context | TECHNICAL CHALLENGE bundle cost + veille effect@3.21.2 npm | ✅ |
| 2. Reformulate | REFORMULATION (4 points) avant chaque turn multi-fichiers | ✅ |
| 3. TDG | 3 fichiers tests ecrits RED avant sources | ✅ |
| 4. Code | Atomic commits (hotfix B-017 separe puis B-019) | ✅ |
| 5. Lint | `biome check packages/engine/src/effects/ tests/effects/` → 0 erreurs | ✅ |
| 6. Tests | Full suite 1116 → **1136 passants** (zero regression). `src/effects/` coverage 96.29% stmts / 100% branches / 95% funcs / 96.15% lines | ✅ |
| 7. Security | PII scan `src/effects/` (jean-pierre/goncalves/jaypee/...) → 0 hits. Grep `from 'effect'` → 3 fichiers, tous sous `src/effects/` (0 KB core leak) | ✅ |
| 8. Verify | `pnpm build` → green. `dist/effects/` contient les 4 modules + .d.ts. Test runtime verifie via failure injection `vi.spyOn` + `vi.doMock` | ✅ |

### Commits

| SHA | Type | Description |
|-----|------|-------------|
| 9762bb6 | fix | e2e-crypto.ts noUncheckedIndexedAccess (B-017 build regression) |
| 19c4a87 | feat | B-019 Effect-TS resilience Storage+Crypto |
| bd7ebec | docs | PET backfill SHA |

Pushed to `origin/main`.

## Scope reduction (vs CDC F-018 initial)

| Module CDC | Statut B-019 | Justification |
|------------|--------------|---------------|
| Storage | ✅ Wrapped | Use-case principal Effect |
| Crypto bridge | ✅ Wrapped | Demontre les 2 signatures (`never` / `CryptoError`) |
| Init | ⏸ Skipped | Mostly sync, seule async passe par Storage |
| Sync engine | ⏸ Defere B-019b | CRDT lifecycle (Y.Doc + Persistence + WS) merite Streams + Scope dedies |
| Telemetry | ⏸ Couvert B-022 | Brick separe |

Decision documentee CDC §Historique et PET §7.

## Erreurs rencontrees

1. **beforeEach storage test hang** — `indexedDB.deleteDatabase` bloque sur connexion singleton ouverte. Fix : adopter pattern `idb-storage.test.ts` (`closeMorphicDB()` AVANT `deleteIdb()`).
2. **Mauvais nom localStorage** dans test migration (`morphic-engine-prefs` au lieu de `morphic-prefs`). Fix : lire la constante source.
3. **B-017 build regression decouvert sous B-019** — `e2e-crypto.ts` ligne 233 + 242 : `bytes[i]` typé `number | undefined` sous `noUncheckedIndexedAccess`. Vitest passait (esbuild = transpile-only), tsc echouait. Hotfix commit separe `9762bb6` AVANT B-019. **Leçon** : Gate 8 (Verify) B-017 doit inclure `pnpm build`, pas seulement `pnpm test`.

## Suivi

- Tag git `morphic-v2.0.0-brick-019` differable (B-018 deja tagué — cadence 3-4 bricks)
- Prochaine brick : B-020 Web Workers (Sensitive 90%)
- B-019b (sync-engine Effect wrappers) en option si la valeur se demontre

## Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| Value | 90 | B-019 livre (resilience layer prod-ready, 0 KB core impact). Hotfix B-017 bonus. |
| Reliability | 85 | 2 corrections mineures intra-session (beforeEach hang + localStorage key) + 1 regression decouverte (B-017 build). Zero regression sortie. |
| Process | 95 | 8 gates respectees. Reformulations multiples. TDG strict (RED→GREEN). Scope reduction documentee (pas de silent drop). -5 pour TECHNICAL CHALLENGE B-017 emis apres avoir commence le fix au lieu d'avant. |
| **Total** | **90** | Value × 0.4 + Reliability × 0.3 + Process × 0.3 = 36 + 25.5 + 28.5 |

## Methodologie — observations

- **TECHNICAL CHALLENGE B-017** : decouverte tardive (apres avoir tente le build). Aurait du etre emis AVANT de toucher e2e-crypto.ts (Honesty.md §Active Technical Challenge format). Note pour prochaine session : devant un blocage de build pre-existant, emettre le challenge avant de fixer.
- **Pattern setup/teardown IDB** : a copier-coller depuis le test de reference (idb-storage.test.ts) au lieu de re-inventer un beforeEach minimaliste. Convention emergente — pourrait merite un helper partage si plus de test files attaquent IDB.
- **Gate 8 Verify** : ce brick valide la maxime « la preuve, jamais l'affirmation » (Monozukuri #5). Le build aurait laisse passer la regression B-017 si on s'etait contente de `pnpm test`.
