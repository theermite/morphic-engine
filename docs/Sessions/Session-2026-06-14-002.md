# Session 2026-06-14-002 — Android Phase A : A-5 (onboarding digne) + A-6 (distribution)

> Projet : morphic-engine (cible Android native). Branche `main`. Suite de 001 (A-3/A-4).
> Modèle : VPS écrit → CI prouve (pas de SDK Android local) → Jay valide en app réelle.

## 1. Travail réalisé

| Brique | Livré | Commit | Preuve CI |
|--------|-------|--------|-----------|
| **A-5** Onboarding sensoriel-AVANT-identité | `Onboarding.kt` (machine pure + guard) + `MorphicOnboardingScreen.kt` (Composable accessible) + 21 tests | `24cc0b9` | CI 27495115010 + android 27495115028 |
| **Couverture auditable** | Rapport Kover (HTML+XML) uploadé en artefact CI | `0a0d34c` | run 27495606377 |
| **A-6** Distribution | Pipeline publication Maven Central (vanniktech) + app `:sample` + dry-run | `8cc16d4`+`221363a`+`8b65be3` | run 27499275603 |

Commits docs « Done » : `7867a43` (A-5), `3b6540e` (couverture mesurée A-5), `c4321ff` (A-6).
Revert : `762d46f` (enforcement Kover cassé, voir §3).

### A-5 — Onboarding digne
- Machine d'état pure Kotlin (idle→theme→motion→density→completed), miroir du web `onboarding.ts`.
- Guard `canCollectIdentity(state)` **pur** → table MC/DC testable (4 combos TT/TF/FT/FF). Contrat Dignity §a BLOCKING (zéro identité avant sensoriel).
- Gardes d'ordre AVANT mutation (snapshot-stable sur valeur invalide). Reset RGPD-friendly (préserve les choix d'axes).
- **Décision Jay** : livrer aussi l'écran Compose fin accessible (heading TalkBack, contentDescription, cibles ≥48dp), pas seulement la logique. Non flooré §5.bis, validé en vraie app.
- **Couverture mesurée 100%** sur la logique (lu dans l'artefact Kover run 27495606377 : `MorphicOnboarding` 37/37, etc.), pas « par construction ».

### A-6 — Distribution
- Publication via vanniktech maven-publish 0.36.0 (Central Portal, AGP 9). Coordinates `com.theermite.morphic:morphic:0.1.0`, POM Apache-2.0, zéro PII (dev = handle public `theermite`).
- App `:sample` (provider A-4 + onboarding A-5 + guard) = vraie app de validation TalkBack/Accessibility Scanner. activity-compose 1.13.0.
- Dry-run CI (`publishToMavenLocal` + `:sample:assembleDebug`) prouve l'artefact + la compilation du drop-in.
- **Publish réel différé** : compte Central Portal + namespace + GPG + secrets côté Jay → `android/PUBLISHING.md` + `release-android.yml` (inerte sur tag).
- **Décisions tranchées par Jay** : licence **Apache-2.0** (sous-arbre `android/`, web reste AGPL), groupId **`com.theermite.morphic`**.

## 2. Tests

| | |
|---|---|
| Web (vitest) | **1350 verts** (engine 1309 + adapter 41) + 9 Rust. Non touché cette session. |
| Android local (VPS) | **N/A** — pas de SDK (modèle acté). |
| Preuve Android | **CI verte** : A-5 (lint+test+kover), couverture artefact, A-6 (dry-run publish + sample assemble). |
| Nouveaux tests | A-5 : 21 (MC/DC guard 4 combos + machine : ordre, skip, rejet valeur, reset). |

## 3. Erreurs rencontrées + corrections

| Problème | Correction |
|----------|-----------|
| **Enforcement Kover (95% scopé) a cassé main** : la doc web affirmait « filtres par règle = oui », faux. Vérifié sur l'API publique `KoverReportsConfig.kt` : `KoverVerifyRule` n'a **pas** de `filters` en 0.9.x → script ne compile pas. | **Revert immédiat** (`762d46f`) pour restaurer main au vert. Puis option auditable choisie par Jay (artefact CI) au lieu du gate bloquant. Leçon `[[feedback_veille-probable-insufficient]]` re-confirmée. |
| **A-6 dry-run rouge** : `signMavenPublication` « no configured signatory » — `signAllPublications()` signait aussi le publish local sans clé. | Signature conditionnée à la présence de la clé (`8b65be3`). Risque anticipé dans le plan. |

Deux corrections, propres. **1 incident notable** : main rouge ~quelques minutes (Kover) avant revert.

## 4. Décisions

1. **A-5 écran Compose en plus de la logique** (Jay) — livre le contrat accessibilité CDC §6.bis.
2. **Couverture auditable via artefact CI** (Jay) — Kover 0.9.x ne permet pas un gate 95% scopé ; le chiffre est lu, pas enforced.
3. **Licence Apache-2.0** pour le sous-arbre Android (Jay) — réutilisation large > cohérence AGPL.
4. **groupId `com.theermite.morphic`** (Jay).
5. **Publish réel différé** — setup Central Portal côté Jay.

## 5. Contexte / santé session
- Session longue (A-5 + A-6 + détour Kover). Pas de compaction signalée. Friction hooks récurrente (reformulate-gate, MANDATORY READ, veille) — recovery propre à chaque fois.
- Glitch de flux conversationnel (l'écran de Jay s'était figé avant la fin d'un message) — rattrapé en re-synchronisant l'état.

## 6. Métriques objectives
- Commits non poussés : **0** (tout sur `origin/main`).
- TODO/FIXME ajoutés : **0**.
- Marqueurs veille : réels et **re-vérifiés en direct** (vanniktech CHANGELOG, activity-compose developer.android.com, Kover API publique, POM). Aucun « probable ».

## 7. Pending (prochaine session)
1. **Clone local + install téléphone (Jay)** — prochaine session en local : cloner le repo, builder `:sample`, l'installer sur le téléphone de Jay pour valider l'onboarding (TalkBack, ≥48dp, Accessibility Scanner). **Consigne explicite de Jay 2026-06-14.**
2. **Publish réel Maven Central** — quand Jay a fait le setup (`android/PUBLISHING.md`).
3. **Phases ultérieures différées** (PET §6) : axes cognitifs, aide lecture/daltonisation, profil holistique, sync E2E, iOS.
4. **Redéploiement The-Ermite** sous `@theermite/*` + release v2.0.0 web (track web, reporté).

## 8. Visibilité
Deux candidats notés dans `Contenu.md` :
- Article « Publier une lib Android sur Maven Central sans SDK sur la machine de build (CI-only + dry-run) ».
- Article « Onboarding sensoriel-avant-identité : un guard testé MC/DC pour la dignité utilisateur (web + Android) ».

## 9. Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| **Value** (40%) | 89 | Phase A Android **terminée** : A-5 (brique la plus exigeante, 100% couverture) + A-6 (distribution + sample) — le module est livrable. |
| **Reliability** (30%) | 80 | 2 corrections propres, 0 régression persistante. **Mais** main rouge quelques minutes (Kover) avant revert = incident réel. |
| **Process** (30%) | 88 | TDG (A-5 tests d'abord), commits atomiques, veille re-vérifiée en direct (trap « probable » évité ×2), plan par brique, PET à jour, reporting honnête de l'incident. |
| **Total** | **86** | (89×0.4)+(80×0.3)+(88×0.3) = 35.6+24+26.4 |

## 10. Conformité
- Veille/SKB : OK (marqueurs réels, sources primaires : API Kover, POM, CHANGELOG vanniktech, developer.android.com).
- Monozukuri : 6/6 (commits achevés, rigueur, erreurs lues, traçabilité, preuve CI + couverture mesurée, choix tenant 6 mois — versions vérifiées en direct).
- Dignity : A-5 EST l'application directe (sensoriel avant identité, guard MC/DC, écran accessible).
- Confidentialité : zéro PII dans le POM / les docs (dev = handle public).
